name: Build Workflow
on:
  push:
    branches:
      - master

jobs:
  build:
    name: Build APK

    runs-on: ubuntu-latest
    steps:
      - name: "Build APK"
        uses: msfjarvis/setup-android@0.2
        with:
          entrypoint: ./gradlew
          args: dependencies assembleDebug